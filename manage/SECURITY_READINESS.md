## セキュリティ・運用 TODO（商用リリース視点）

**目的**: 「趣味開発としては十分」から、「お金を取るゲームとして外に出しても耐えられる」状態までに必要な確認事項をまとめる。

優先度ラベル:
- **[NOW]**: できるだけ早く（MVP前でも）やっておくと、後からの破綻リスクを下げられるもの
- **[PRE-PAID]**: 有償通貨や課金を入れる前までに必須
- **[LATER]**: 規模が出てきたら順次でOK

---

## 1. 認証・セッション

- ~~[NOW] **SESSION_SECRET の必須化**~~ （**コード対応済み: 2026-03-01**）
  - `src/lib/auth/session.ts` で、本番 (`NODE_ENV=production`) かつ `SESSION_SECRET` 未設定／32文字未満なら起動時にエラーにするよう変更済み。
  - **本番デプロイ時の TODO**: デプロイ先の Env 画面で `SESSION_SECRET` を 32 文字以上のランダム値（`openssl rand -base64 32` 等）として設定する。
- [NOW] **セッション設定の最終確認**
  - `iron-session` の `cookieOptions` が `httpOnly: true` / `secure: NODE_ENV === 'production'` / `sameSite: 'lax'` になっていることを確認。
- [PRE-PAID] **ログイン試行のレートリミット**
  - 同一 IP / email に対する連続ログイン失敗の制限（例: 数分あたり N 回まで）。
  - 簡易的な失敗回数カウントと、異常なパターンの検知（運営用ログ）。

---

## 2. 通貨・課金・取引ログ

- [PRE-PAID] **通貨増減のトランザクションログ設計**
  - `CurrencyTransaction`（または同等）のテーブルを用意し、通貨が増減するすべての処理でレコードを追加する。
    - カラム例: `userId`, `beforeBalance`, `delta`, `afterBalance`, `reasonCode`, `externalId (stripeChargeId 等)`, `createdAt`.
  - 通貨残高の更新は必ずこのロジック経由に統一する。
- [PRE-PAID] **決済プロバイダとの連携前提の設計**
  - どのプロバイダ（Stripe 等）を使うかを決め、Webhook 検証（署名チェック）を前提にしたフローを設計。
  - クライアントから「増やしたい通貨量」を受け取らず、サーバ側で SKU → 金額 → 付与量を決定する。

---

## 3. 機能ごとの濫用対策（レートリミット等）

- [LATER] **チャット送信のレートリミット**
  - userId / IP ごとに、一定時間あたりの投稿数を制限する（スパム・荒らし対策）。
- [LATER] **クラフト・探索実行のレートリミット**
  - 通貨や在庫が絡むアクション（クラフト、探索開始など）に対して、極端な連打を防ぐ簡易制限を入れる。

---

## 4. 運用・インフラまわり

- [NOW] **バックアップとリストア手順の確認（ローカル or ステージング）**
  - DB のバックアップ取得方法と、別環境でのリストア手順を 1 回通しておく。
  - 「本番データを誤って壊した場合に、どのくらいで戻せるか」の目安を把握する。
- [LATER] **エラーログ・監視**
  - 致命的エラー（500）やレスポンス遅延を検知できる仕組み（Apm/Sentry 等）を導入する。

---

## 5. 権限・運営用機能

- [LATER] **運営用ビュー / コマンド**
  - 特定ユーザーの通貨履歴を確認する簡易ビュー。
  - 異常な通貨増減や行動パターンをざっくり見るための集計（後回しでも可）。

---

## メモ

- 現状は「趣味開発としては健全〜堅実寄り」の状態。  
  上記の [NOW] と [PRE-PAID] を押さえれば、「少人数向けの課金ありゲーム」としてもかなり安心感が高まる想定。

