# 本番用 DATABASE_URL
# このファイルを production.env にコピーし、1行目に本番の接続URLを記入する。
# production.env は .gitignore 済み。Cursor に本番リリースを依頼するときは
# この production.env を参照してもらう（PRODUCTION_RELEASE_GUIDE.md §9）。
#
# DATABASE_URL=postgresql://user:password@host:port/database

DATABASE_URL=
